spring.application.name=index-crm-api
server.port=${PORT:8080}

# --- CONEXÃO COM O SUPABASE (POOLER) ---
# Note o prefixo 'jdbc:postgresql' e a porta 6543
spring.datasource.url=jdbc:postgresql://aws-1-us-east-1.pooler.supabase.com:6543/postgres

# O usuário ESPECÍFICO do Pooler que você me mandou
spring.datasource.username=postgres.sitxxezejqibobsppmub

# COLOQUE AQUI A SENHA QUE VOCÊ CRIOU (SEM ASPAS E SEM [])
spring.datasource.password=MinhaSenhaForte2026

spring.datasource.driver-class-name=org.postgresql.Driver

# --- JPA ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- SEGURANÇA ---
api.security.token.secret=${JWT_SECRET:segredo-padrao-dev-123}
api.security.cors.origin=${FRONTEND_URL:http://localhost:3000}