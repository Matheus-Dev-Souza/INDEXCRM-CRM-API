spring.application.name=index-crm-api
server.port=${PORT:8080}

# --- CONEXÃO COM O SUPABASE ---
# Repare que adicionei o prefixo 'jdbc:' antes de 'postgresql://'
spring.datasource.url=jdbc:postgresql://db.sitxxezejqibobsppmub.supabase.co:5432/postgres

spring.datasource.username=postgres

# Coloque a senha ENTRE ASPAS DUPLAS para garantir que o Java leia o '#' final
spring.datasource.password="nX5dJ?HSXvvJNG#"

spring.datasource.driver-class-name=org.postgresql.Driver

# --- JPA ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- SEGURANÇA ---
api.security.token.secret=${JWT_SECRET:segredo-padrao-dev-123}
api.security.cors.origin=${FRONTEND_URL:http://localhost:3000}